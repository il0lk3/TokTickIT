# Lab 2 Sprint Engineering Specification

## 1. Sprint Goal
The goal of this sprint is to deliver a functional MVP for the Requester ticketing experience. This includes enabling requesters to select a testing identity, create IT support tickets with file attachments, view a paginated list of their tickets, and inspect ticket details. This serves as the foundation before full authentication is introduced in Lab 3.

## 2. Stakeholder Request Interpretation
The IT department needs a robust frontend and backend system for users to submit issues. Because we lack real login for now, we will simulate it using a "Development Requester Selection" screen. The system must allow creating tickets, attaching files (up to 5MB, specific formats), viewing a list of tickets with search/filter/pagination, and looking at ticket details. The UI must follow the "Zen Green Theme" strictly.

## 3. Scope
### Included
- Development Requester selection context mechanism (simulated login).
- Create Ticket workflow with data validation.
- My Tickets workflow (paginated list, search, filtering, sorting).
- Requester Ticket Detail workflow (read-only view of ticket).
- Attachment lifecycle (upload, download, metadata viewing, soft removal).
- Zen Green UI implementation and responsive design.

### Excluded
- Real authentication and security (login, passwords, sessions, JWTs, roles).
- IT Staff workflow (staff dashboards, claiming tickets, prioritizing).
- Ticket collaboration (public comments, internal notes, actions taken).
- Ticket lifecycle beyond creation (resolving, closing, reopening).
- Administration functions.

## 4. Functional Requirements
- **FR-01 (Requester Selection):** The system must provide a screen to select an active Development Requester to simulate the current user context.
- **FR-02 (Create Ticket):** A selected Requester must be able to create a ticket by providing Category, Related System, Priority, Summary, Description, and optional Attachments.
- **FR-03 (My Tickets):** A selected Requester must be able to view a paginated list of their own tickets, with capabilities to search, filter by category/priority/status, and sort.
- **FR-04 (Ticket Detail):** A selected Requester must be able to view the read-only details of their own ticket.
- **FR-05 (Cross-Requester Protection):** The system must prevent a Requester from viewing or modifying tickets or attachments belonging to another Requester.
- **FR-06 (Attachment Addition):** A Requester can add up to 5 valid attachments (JPG, PNG, WEBP, PDF; max 5MB each) to a new or existing ticket.
- **FR-07 (Attachment Removal):** A Requester can soft-remove an attachment from their own ticket, making it inaccessible for download while retaining metadata.

## 5. Business Rules
- **BR-01 (Ticket Number):** The official Ticket Number is generated by the backend and must be unique (TKT-YYYY-NNNNNN).
- **BR-02 (New Status):** A new Ticket begins with Current Status 'New'.
- **BR-03 (Simulated Auth):** Lab 2 uses a Development Requester selector instead of login. The selected identity is for testing only and is not authentication.
- **BR-04 (Attachment Limits):** Attachments are limited to JPG/JPEG, PNG, WEBP, and PDF. Maximum size is 5 MB per file. Maximum active attachments is 5 per Ticket.
- **BR-05 (Soft Removal):** Removed files must be implemented as a soft removal. Removed files must not be downloadable or previewed, but metadata remains visible.
- **BR-06 (Character Limits):** Ticket Summary and Description are required fields with appropriate length constraints (Summary max 150 chars, Description max 1000 chars).
- **BR-07 (Idempotency):** Ticket creation must be idempotent or protected against duplicate submissions while loading (button disabled during submit).

## 6. UI Specification Summary
The interface strictly follows the "Zen Green Theme" detailed in `ui-spec.md`.
- **Colors:** Primary `#006B3C`, Secondary `#0B7A46`, Pale `#EAF6EF`, Read-only shading `#F4F7F6`.
- **Application Shell:** Features a navigation bar with the active Requester's name and links to My Tickets and Create Ticket.
- **Responsive Layouts:** Desktop uses multi-column layouts, Tablet uses two-column, Mobile stacks vertically.
- **Key Screens:** Development Requester Selection, Create Ticket (form), My Tickets (list/table), Ticket Detail (read-only).

## 7. Data Changes
The PostgreSQL database (via Prisma) includes the following models:
- **RequesterUser:** Stores Development Requesters (`id`, `name`, `email`, `isActive`).
- **Category:** Reference data.
- **RelatedSystem:** Reference data.
- **Ticket:** Core entity (`id`, `ticketNumber`, `requesterId`, `categoryId`, `relatedSystemId`, `summary`, `description`, `requestedPriority`, `currentStatus`, `createdAt`, `updatedAt`).
- **Attachment:** File metadata (`id`, `ticketId`, `filename`, `originalName`, `mimeType`, `size`, `isRemoved`, `removedReason`, `createdAt`).

## 8. API Contract
See `api-spec.md` for full details.
Endpoints implemented:
- `GET /api/categories`, `GET /api/systems`, `GET /api/requesters`
- `POST /api/tickets`
- `GET /api/tickets` (Paginated, Filtered)
- `GET /api/tickets/:id`
- `POST /api/tickets/:id/attachments`
- `GET /api/tickets/:id/attachments/:attachmentId/download`
- `DELETE /api/tickets/:id/attachments/:attachmentId`

## 9. Acceptance Criteria
- **AC-01 (Create):** Given valid Ticket data, when the Requester submits the form, then one Ticket is saved and the official Ticket Number is displayed.
- **AC-02 (No Requester):** Given no Development Requester is selected, when the user attempts to open My Tickets, then the Requester Selection screen is shown.
- **AC-03 (Ownership):** Given Requester B is selected, when a Ticket belonging to Requester A is requested, then the Ticket data is not returned (404 Not Found).
- **AC-04 (Validation):** Given the Create Ticket form, when submitted without a Summary, then a field-level error message is displayed and the API is not called.
- **AC-05 (Attachment Limit):** Given a ticket with 5 active attachments, when the Requester attempts to upload a 6th, then the upload is rejected with a clear error message.
- **AC-06 (Soft Removal):** Given an active attachment, when the Requester soft-removes it, then it is marked as removed, remains in the UI as disabled/removed metadata, and the download API returns 410 Gone.
- **AC-07 (Search):** Given the My Tickets screen, when the user searches for a keyword, then only tickets whose summary or ticket number match the keyword are displayed.

## 10. Definition of Done
- [x] Implementation of all approved scope complete on feature branches.
- [x] Satisfaction of all acceptance criteria (AC-01 to AC-07).
- [x] Passing and traceable automated tests (Unit, API, UI, E2E) as planned in `tests.md`.
- [x] Conformance to the data, API, UI, validation, and responsive specifications.
- [x] Correct handling of success, failure, and boundary cases.
- [x] Pull Requests reviewed and merged into `lab2-staging` / `main`.
- [x] Final visual inspection on Desktop, Tablet, and Mobile viewports passes without overlapping or clipping.

## 11. Assumptions and Decisions
- Uploaded files are stored locally in an `uploads` directory for simplicity during this lab.
- The `uploads/` directory is added to `.gitignore`.
- "Inactive" Development Requesters are used to test data integrity but do not appear in the Requester Selector dropdown.
- We added a database seeding script (`server/scripts/seed-tickets.ts`) to populate realistic test data for the UI review.
